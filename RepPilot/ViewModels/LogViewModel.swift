import Foundation
import SwiftData

// LogViewModel is now minimal — AddWorkoutView manages its own state directly.
// Kept for any future shared log-tab state.
@MainActor
@Observable
final class LogViewModel {}
